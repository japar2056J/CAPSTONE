// Riwayat (History) Model Structure
const riwayatModel = {
  productName: String,
  estimatedPrice: Number,
  kurs: Number,
  date: String, // ISO Date
  user: String  // User email
};

module.exports = riwayatModel;
